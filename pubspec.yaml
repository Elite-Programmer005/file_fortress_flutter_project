name: file_fortress
description: Secure File Vault Application
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core
  provider: ^6.1.2
  
  # Security & Encryption
  encrypt: ^5.0.1
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.2.0
  
  # Database
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.0
  
  # File Handling
  file_picker: ^8.0.0
  path: ^1.9.0
  mime: ^1.0.4
  uuid: ^4.2.1 # <-- ADDED THIS PACKAGE
  
  # UI
  flutter_svg: ^2.0.9
  shimmer: ^3.0.0
  
  # Crypto
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
